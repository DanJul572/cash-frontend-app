import { Toolbar } from '@mui/x-data-grid';

import ZTableDownloadButtonComponent from './ztable-download-tollbar-component';

export default function ZTableToolbarComponent() {
  return (
    <Toolbar>
      <ZTableDownloadButtonComponent />
    </Toolbar>
  );
}
