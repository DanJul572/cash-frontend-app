import { createTheme } from '@mui/material/styles';

export const themeConfig = createTheme({
  palette: {
    mode: 'dark',
    primary: {
      main: '#E64A19',
      dark: '#BF360C',
      light: '#FF6D00',
    },
  },
});
